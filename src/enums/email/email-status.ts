export enum SentMailStatus {
  DRAFT = 'draft',
  QUEUED = 'queued',
  PROCESSING = 'processing',
  SENT = 'sent',
  FAILED = 'failed',
  CANCELLED = 'cancelled',
  COMPLETED = 'completed',
  PAUSED = 'paused',
}
