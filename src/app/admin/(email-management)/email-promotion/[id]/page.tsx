'use client';
import React from 'react';

const EmailPromotionDetailsPage = ({
  params: { id },
}: {
  params: { id: string };
}) => {
  return <>{id}</>;
};

export default EmailPromotionDetailsPage;
